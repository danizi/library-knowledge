# D0 卡点 · 2026-08-27

- Android Studio 必须 Open **Gradle 根**：`demos/activity/act-d0-hello`（有 `settings.gradle.kts` / `gradlew`）。
- 打开上层 `demos/` 不会 Sync、没有 Run——那不是工程。
- Manifest 入口：`MainActivity` + `MAIN`/`LAUNCHER`。
- D0 已过：Run OK、旋转不崩。
